get:
  tags:
    - referral
  summary: Список рефералов и дополнительная информация
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/referral.yaml"
  security:
    - USERSID: [ ]