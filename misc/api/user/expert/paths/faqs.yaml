get:
  tags:
    - expert
  summary: Список быстрых ответов
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../schemas/faqs.yaml"
  security:
    - USERSID: [ ]