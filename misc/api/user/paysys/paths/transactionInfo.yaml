post:
  tags:
    - paysys
  summary: Проверка статуса платежа
  deprecated: true
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/transactionInfoRequest.yaml"
    required: true
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/transactionInfoResponse.yaml"
  security:
    - USERSID: [ ]
