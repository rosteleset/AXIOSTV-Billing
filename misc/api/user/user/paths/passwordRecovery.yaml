post:
  tags:
    - user
  summary: Восстановление пароля
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/passwordRecoveryRequest.yaml"
    required: true
  responses:
    $ref: "../schemas/passwordRecoveryResponse.yaml"
