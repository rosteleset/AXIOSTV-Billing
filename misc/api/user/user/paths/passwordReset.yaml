post:
  tags:
    - user
  summary: Изменение пароля пользователем, после восстановления пароля
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/passwordResetRequest.yaml"
    required: true
  responses:
    $ref: "../schemas/passwordResetResponse.yaml"