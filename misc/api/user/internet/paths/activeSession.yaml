get:
  tags:
    - internet
  summary: Активная сессия абонента
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/activeSession.yaml"
  security:
    - USERSID: [ ]