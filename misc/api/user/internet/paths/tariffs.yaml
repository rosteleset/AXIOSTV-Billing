get:
  tags:
    - internet
  summary: Доступные для изменения ТП, на которые хватает денег
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/tariffs.yaml"
  security:
    - USERSID: [ ]