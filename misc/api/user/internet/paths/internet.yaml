get:
  tags:
    - internet
  summary: Информация об интернет услугах пользователя
  responses:
    200:
      description: Успешное выполнение
      content:
        application/json:
          schema:
            $ref: "../schemas/internet.yaml"
  security:
    - USERSID: [ ]